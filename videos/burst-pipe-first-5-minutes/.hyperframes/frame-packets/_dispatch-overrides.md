# Orchestrator overrides (win over the role where they conflict)

1. **Canvas 1920×1080.** Set `data-width="1920" data-height="1080"` on the root. Captions are ENABLED as a separate root track: keep every element of yours above y = 896px (the bottom ~17% is the caption band). Do NOT render captions or the narration sentence yourself.
2. **Timing.** The Scene lines give frame-relative word times from the real voiceover. Land each reveal on its listed time (±0.05s). Your frame `duration` is fixed — never change it.
3. **Illustrations.** Never redraw the kit. Read each SVG you need from `PROJECT_DIR/public/illustrations/` and paste its `<svg>…</svg>` markup INLINE in your template (keep `viewBox`, remove nothing else, size via CSS). Prefix every id inside a pasted SVG with your element prefix (e.g. `v04-wheel`) so ids are unique across the assembled video, and update your selectors accordingly. You may draw small extra glyphs inline (outlet, sound arcs, arrows, swirl, checks) in the same style: cobalt `#1e2bfa` strokes, round caps/joins, cobalt tints for fills.
4. **Fonts — exactly two families.** Only `"Space Grotesk"` and `"Inter"` (generic fallback `sans-serif`). No other family names anywhere in the file. Put this inside your `<template>`: `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600&display=swap">`
5. **Element ids must start with a letter** (use a short per-frame prefix such as `h01-`, `s02-`), never with a digit. The composition id / timeline key stays the frame_id verbatim.
6. **Contrast.** Body/label text ≥ 4.5:1 against its background; use `#111111` or `#6b6b6b` on cream/tints; white only on solid cobalt.
7. **Shared chrome + step layout** exactly as in `_video-direction.md` (eyebrow top-left, tag-pill top-right with this frame's `tag:`, 3px progress bar at the bottom edge growing to (frame number ÷ 9) of the width). Step frames 04–08 use the identical left column / right stage geometry.
8. **Determinism.** One paused GSAP timeline at `window.__timelines["<frame_id>"]`; `fromTo` entrances; no `repeat:-1`, no `yoyo` loops, no `Math.random`, no `Date.now`, no CSS transitions/animations. Water dash flow = one long finite linear tween of `stroke-dashoffset` across the frame.
9. **Premium bar.** Generous whitespace, precise alignment on a 12-column feel, no shadows, no gradients, no emoji. If a Scene line would overcrowd the stage, simplify the supporting elements, never the focal.
10. When done, reply with one line: the file path and a one-sentence summary.
